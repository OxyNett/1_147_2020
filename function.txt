#include <iostream>
#include <cmath> 

using namespace std;

bool i;
float a=2;
float del=0.6;
float x;
float y;
float b[] = {1.16, 1.32, 1.47, 1.65, 1.93};
    
int main()
{
    cout << "Запустить задачу A/B? (1/0)" << endl;
    cin >> i;
    if (i)
    {
    	for (x = 1.2; x<4.3; ) 
    	{
        	y=pow((log10(a+x)),2)/pow((a+x),2);
       	    cout << y << endl;
       	    x=x+del;
    	}
    }
    else
    {
    	for (int c = 0; c<5; c++) 
    	{
        	y=pow((log10(a+b[c])),2)/pow((a+b[c]),2);
       	    cout << y << endl;
    	}
    }
}