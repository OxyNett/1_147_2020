#include <iostream>
#include <conio.h>
#include <windows.h>
using namespace std;
int a;
int arg;
int main()


{
	HANDLE handle = GetStdHandle(STD_OUTPUT_HANDLE);
	SetConsoleTextAttribute(handle, FOREGROUND_GREEN);

	while (!_kbhit())
	{
		cout << arg;
		arg += rand() % 10000;

	}

	return 0;
}
